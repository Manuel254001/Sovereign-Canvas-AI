import * as lit from 'lit';
import { LitElement, PropertyDeclarations } from 'lit';

/**
 * Small lit component to work on popup.
 * This is not being called quite yet but will be the way to support
 * the popup in the future.
 */
declare class ModalPopup extends LitElement {
    static styles: lit.CSSResult;
    static properties: PropertyDeclarations;
    isOpen: boolean;
    iframeSrc: string;
    private iframeElement;
    render(): lit.TemplateResult<1>;
    handleIframeLoad: () => void;
    handleIframeError: (e: ErrorEvent) => void;
    private resolve?;
    private reject?;
    openModal(url: string): Promise<Window | null>;
    firstUpdated(): void;
    disconnectedCallback(): void;
    closeModal(): void;
}
declare function createWalletPopup(): ModalPopup | null;

export { ModalPopup, createWalletPopup };
